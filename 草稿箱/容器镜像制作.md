---
title: "微服务容器镜像制作"
owner: ""
kind: runbook
maturity: draft
risk: medium
tags: [镜像, dockerfile, 构建, 微服务]
related: []
created: 2026-08-18
last_verified: null
last_reviewed: null
---

# 微服务容器镜像制作

## 目标
为微服务（可能有运行库依赖）制作容器镜像。

## 适用范围
- 环境：构建机
- 前提权限：代码仓读权限、镜像仓推送权限

## 前置条件
- 与需求方沟通清楚：运行时依赖、暴露端口、启动命令

## 操作步骤

1. 沟通需求，明确依赖与运行方式
2. 找类似代码仓的 Dockerfile 参考改写
3. 编译打包命令编写调试：
   ```dockerfile
   # 常见问题：缺库安装、基础镜像不匹配 → 多次换基础镜像尝试
   ```
   缺库时先走 `rpm-yum-mirror` 确认依赖在源上的版本
4. 本地跑通后推送镜像仓（TODO：镜像仓与命名规范）

## 验证
- `docker run` 起容器，服务进程正常、端口可访问
- 镜像在仓库可见且 tag 正确

## 回滚
- 删除错误 tag 的镜像；不影响存量服务

## 常见问题
- 缺库：换基础镜像或补充安装步骤
- 编译打包命令与目标 os 不兼容：对齐 os 版本

## 关联
- runbooks：TODO rpm-yum-mirror（同批导入）
